# ── macOS / Swift ─────────────────────────────
macos/.build/
macos/build/
macos/.swiftpm/
Package.resolved
*.xcodeproj
*.xcworkspace
DerivedData/

# ── Windows / .NET ────────────────────────────
windows/**/bin/
windows/**/obj/
*.user
.vs/

# ── 运行时数据（绝不入库）──────────────────────
# 真实配置里有服务器地址和私钥路径；数据库是本机采集结果
config.json
usage.sqlite*
secrets.bin

# ── 凭据（永远不该出现在仓库里）────────────────
# 本机代码签名身份：证书指纹与持有人绑定，公开仓库里不该有
macos/scripts/signing-identity.local
*.pem
id_rsa*
id_ed25519*

# ── 系统 ──────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
# VS Code 的 launch.json 由 Swift 扩展按 Package.swift 的 target 自动生成，
# 装了扩展的人打开项目就会有，入库只会多一份要跟着 target 维护的文件
.vscode/
*.swp
