# 更新日志

本文件记录值得用户关注的变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **鼠标移到菜单栏图标 / 托盘图标上就浮出面板**，不必再点一下。
  移开或点击面板外即收起；点击图标打开的面板会钉住，不跟着鼠标消失
- **可以指定常驻区显示哪一台服务器的剩余流量**（设置 → 菜单栏 / 托盘显示）。
  新增共享配置字段 `menuBarServerId`，两端通用；不指定时仍跟着用量最紧张的那台走
- 同一个下拉里新增「**不显示**」：macOS 菜单栏只剩图标，Windows 右键菜单不再有那行摘要，
  图标颜色照常随严重程度变化。对应共享配置字段 `menuBarShowsRemaining`
- **开机自启开关**（设置 → 启动），不必再手工添加登录项或往 `shell:startup` 放快捷方式。
  macOS 走 `SMAppService` 登记到「系统设置 → 通用 → 登录项」，
  Windows 写当前用户的注册表启动项（`HKCU\...\CurrentVersion\Run`，不需要管理员权限）。
  默认关闭；状态以系统为准，在系统设置 / 任务管理器里改动后，重新打开设置窗口即可看到

### 变更

- 菜单栏图标旁的数字由**用量百分比**改为**所选服务器本账期的剩余流量**；
  与面板同一套换算（≥1 TB 用 T、≥1 GB 用 G、其余用 M），只是单位缩成一个字母、
  不留空格：`1.94T`、`576G`、`512M`。该服务器没有配额时显示一道短横
- Windows 托盘不再使用系统 tooltip，改由悬停浮窗承载；
  右键菜单首行新增只读摘要「服务器名　剩余 xx GB / 配额」
- Windows 托盘图标的颜色与填充比例改为反映「设置里指定的那台」，
  与菜单上的数字保持一致
- macOS 菜单栏项改由自建的 `NSStatusItem` 实现（原先是 SwiftUI 的 `MenuBarExtra`）——
  `MenuBarExtra` 只响应点击，拿不到状态项按钮就无法追踪鼠标悬停

## [1.0.0] - 2026-09-08

首个公开版本。macOS 与 Windows 两端功能对等，共用同一份配置格式和本地数据库结构。

### 新增

**核心能力**

- 监控多台 VPS 的月度流量配额，按当前速度外推账期末用量并判断是否会超额
- 两条采集路径归一为「按天的 rx/tx 序列」：
  - **Vultr** —— 官方 REST API（`/v2/instances` 取 `allowed_bandwidth`，
    `/v2/instances/{id}/bandwidth` 取每日进出字节数）
  - **SSH + vnstat** —— 适用于任何可 SSH 的服务器（DMIT 无公开 API，走这条）
- 本地 SQLite 覆盖式 upsert 留存历史，不受上游数据窗口限制
  （Vultr 约 30 天、vnstat 默认 62 天）
- 按账单重置日切分账期，`resetDay` 支持 1–31 并在当月天数不足时回退到月末
- 四种计费口径（仅出站 / 仅入站 / 双向求和 / 取较大值）与两种 GB 进制（1024³ / 1000³）
- 并发采集，单台失败不影响其他台，失败原因与上次成功时间可见
- **起始已用量补偿** —— 账期中途才接入时，可从服务商面板抄一个已用量补齐缺口。
  该基准绑定具体账期，换账期后自动失效
- API Key 不写入配置文件：macOS 存系统钥匙串，Windows 用 DPAPI 加密

**macOS 端**

- 菜单栏常驻，图标直接显示最紧张那台的用量百分比
- 主窗口（服务器列表 + 详情与趋势图）
- **三条兜底入口** —— 菜单栏拥挤时 macOS 会静默丢弃放不下的状态项（带刘海的机型尤其
  容易触发），因此另有 Dock 图标与 `open -a VPSQuota`；「呈现方式」可在
  仅菜单栏 / 仅 Dock / 两者之间切换，默认两者都开
- 应用图标由代码绘制（`scripts/make-icon.swift`），小尺寸下自动简化图形
- 「帮助 → 使用说明」打开随包分发的文档；打包时若有 pandoc 则渲染为带样式的 HTML，
  没有则退回原始 Markdown
- 简体中文本地化（含系统标准菜单）
- 诊断 CLI `vpsquota-cli`：`set-key` / `vultr-instances` / `probe` / `refresh` /
  `status` / `config-path`，接入真实凭据时可绕开界面逐项排查

**Windows 端**

- 系统托盘常驻，图标颜色随用量变化
- 主窗口与设置界面，功能与 macOS 端对等

### 修复

- 设置界面「常规设置」点不动 —— 它的 tag 与「什么都没选」是同一个值，
  SwiftUI 无从区分。改用独立枚举消除二义性
- 设置界面左右比例失衡，改用 `NavigationSplitView` 并显式约束列宽
- 趋势图横轴在只有一天数据时退化成小时轴，改为固定按账期范围显示；
  账期内缺失的日子保留为空白 —— 数据缺口本身就是需要看见的信息
- 累计曲线未计入起始已用量，导致与头部数字打架，改为从基准起步
- 单日数据画不出线段，补 `PointMark`
- 帮助菜单指向一本并不存在的帮助书，点击只弹 "Help isn't available for …"
- 主窗口默认选中等在整轮采集之后，打开会空白好几秒；改为本地数据一到就选中
- 设置窗口在启动时自动弹出，用 `defaultLaunchBehavior(.suppressed)` 抑制

### 已知限制

- **不是对账工具。** 读的是网卡计数或 API 报告值，与服务商交换机上的计费计数
  通常接近但不相等。逼近配额时以服务商面板为准
- `meterMode` 与 `unitBase` 填错会造成系统性偏差（前者可能差一倍，后者差 7.4%），
  首次接入必须对照服务商面板校准
- vnstat 按服务器本地时区切分自然日，本工具按 UTC 记账，非 UTC 服务器有数小时偏差
- macOS 端为 ad-hoc 签名，每次重新构建签名都会变，首次读取钥匙串时会弹授权框
- Windows 端的 DPAPI 密文换机器或换用户后无法解开，需要重新填写 API Key
- Windows 端暂无自动化测试

[1.0.0]: https://github.com/striver2006/vps-traffic-quota/releases/tag/v1.0.0
