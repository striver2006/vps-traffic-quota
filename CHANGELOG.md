# 更新日志

本文件记录值得用户关注的变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **鼠标移到菜单栏图标 / 托盘图标上就浮出面板**，不必再点一下。
  移开或点击面板外即收起；点击图标打开的面板会钉住，不跟着鼠标消失
- **可以指定常驻区显示哪一台服务器的剩余流量**（设置 → 菜单栏 / 托盘显示）。
  新增共享配置字段 `menuBarServerId`，两端通用；不指定时仍跟着用量最紧张的那台走
- 同一个下拉里新增「**不显示**」：macOS 菜单栏只剩图标，Windows 右键菜单不再有那行摘要，
  图标颜色照常随严重程度变化。对应共享配置字段 `menuBarShowsRemaining`
- **开机自启开关**（设置 → 启动），不必再手工添加登录项或往 `shell:startup` 放快捷方式。
  macOS 走 `SMAppService` 登记到「系统设置 → 通用 → 登录项」，
  Windows 写当前用户的注册表启动项（`HKCU\...\CurrentVersion\Run`，不需要管理员权限）。
  默认关闭；状态以系统为准，在系统设置 / 任务管理器里改动后，重新打开设置窗口即可看到
- **Windows 端有单元测试了**（`windows/VpsQuota.Tests`，CI 会跑）。用例与 macOS 端
  逐条对应、期望值逐字相同，两边读同一份 `shared/schema.sql` 与 `shared/config.example.json`
  —— 改了任一端的账期切分或口径折算而没同步另一端，对面的 CI 就会红。
  这是验收标准「两端对同一份输入给出相同结果」第一次有了自动化保障
- **采集错误与上游配额会留存到本地**：关掉应用再打开，一台一直连不上的服务器
  仍然显示着上次的失败原因，配额填 0 的 Vultr 实例也不会先显示成「配额未知」
- **账期切换当天界面会自动翻页**：此前只有在下一次采集完成后才更新，
  刷新周期设成「每天」时，账期都换了大半天，界面还停在上一期的累计值上
- 服务器上装的是 vnStat 1.x 时，直接说明版本不兼容并给出升级命令 ——
  此前会报「还没有可用的日流量数据」，把人引向「再等一天」这条错路
- Vultr 实例列表支持翻页；账号实例超过一页时，后面那些不再静默变成「配额未知」

### 修复

- **菜单栏面板每悬停一次就会重新采集一轮**：启动流程原先挂在面板视图上，
  而面板每次浮出都算一次"出现"。副作用有三个 —— 每次划过图标都去 SSH 登录所有机器、
  自动刷新的定时器被反复重建以致永远轮不到触发、面板收起时那次被取消的采集
  还会被记成一条"失败"显示出来。现在启动流程只在应用启动时跑一次
- **钥匙串暂时读不出来时，API Key 会被静默删除**：读取失败原先与"没有设置"混为一谈，
  之后只要开关一次设置窗口，那条真实的密钥就会被当成"用户清空了"而删掉。
  登录时启动尤其容易触发（那一刻钥匙串还没解锁）。现在这种情况下不会去动已存的密钥，
  设置界面也会说明
- **config.json 解析失败时会被空配置覆盖**：原先"文件损坏"与"文件不存在"走同一条路径，
  界面显示"还没有配置服务器"，用户以为要重配，而任何一次保存都会把那份还能抢救的文件盖掉。
  现在解析失败会明确报错并**拒绝写入**
- **Windows：新增第二台服务器时，字段被上一台污染**。列表刷新触发的事件会把
  表单里还留着的上一台的内容写进新建的那台，默认值全部作废
- **Windows：刷新期间改配置会导致进程崩溃**（采集途中增删服务器触发集合被修改异常），
  且刷新入口没有异常兜底 —— 数据库层面的任何故障都会让应用直接消失，
  而不是像设计约定的那样"报出原因、继续显示上次的数据"
- **配置文件的两端容忍度不一致**：`id` / `name` / `provider` 在 macOS 端是必填、
  在 Windows 端有默认值，于是漏填 provider 的配置在 Windows 上能跑、
  拷到 macOS 却整份读不出来。现在两端一致：坏掉的条目跳过并提示有几台没读进来，
  其余服务器照常工作；字段名两端都严格区分大小写；显式 `null` 一律当作"没填"
- **Windows：未保存的设置改动已经生效于正在运行的采集**。删掉的机器立刻不再被采集、
  新加的立刻开始采集，只是没写进配置文件，重启后又"复活"。现在改成编辑草稿、保存时才提交
- **Windows：「测试连接」会隐式保存全部配置**，点完再关窗口也放弃不掉改动
- **Windows：任意一台采集失败就把托盘图标整个变灰**，指定显示的那台已经 92% 也看不出来
- **Windows：同一次编辑里既改重置日又填起始已用量时，补偿会静默失效**（账期戳用了旧的重置日）
- **Windows：重复双击 exe 会起第二个实例**（两套定时器、两个进程写同一个数据库）。
  现在会把已有窗口唤到前面来 —— 托盘图标被系统收进溢出区时，这也是找回应用的办法
- **Windows：托盘浮窗在副屏上会跑到主屏角落**（定位只看主显示器的工作区）
- **Windows：点击收起浮窗后，鼠标一动它又弹回来**
- **Windows：删掉最后一台服务器后，主界面右侧仍残留着它的账期与错误信息**
- **Windows：密钥文件不是原子写**，写入中断会留下半个文件，下次读取当作"还没设置"
- 两端的 SQLite 都没有设置忙等超时：图形界面与诊断 CLI 同时打开同一个库时，
  一次本来成功的采集会被记成失败
- 两端都不再接受以 `-` 开头的 SSH 地址（会被 ssh 当成选项解析）
- 两端在用量比例相同时选择同一台服务器（原先一个取最后一台、一个取第一台）
- **macOS：采集被取消时不再拖着不放**。外层放弃采集后，ssh 进程会被立刻停掉
  （必要时强制结束），而不是继续跑满超时；忽略终止信号的进程也不会再把
  「正在采集…」永久卡住
- **macOS：命令输出偶发解析失败**。收尾时没等两个管道都读完，
  内容可能被截断或次序错乱，表现为间歇性的「vnstat 输出解析失败」
- **Windows：趋势图里的每日柱状被累计曲线压平**。两者共用一条纵轴，
  账期后期柱子普遍不到 3% 高，而「某天突然跑量」正是这张图要看的东西；
  现在柱状有自己的量级
- Windows 的账期与时间戳计算显式按 UTC 归一，不再依赖调用方传对时间类型
- 每轮刷新只拉一次 Vultr 实例列表，多台 Vultr 时不再重复请求同一份数据

### 变更

- 菜单栏图标旁的数字由**用量百分比**改为**所选服务器本账期的剩余流量**；
  与面板同一套换算（≥1 TB 用 T、≥1 GB 用 G、其余用 M），只是单位缩成一个字母、
  不留空格：`1.94T`、`576G`、`512M`。该服务器没有配额时显示一道短横
- Windows 托盘不再使用系统 tooltip，改由悬停浮窗承载；
  右键菜单首行新增只读摘要「服务器名　剩余 xx GB / 配额」
- Windows 托盘图标的颜色与填充比例改为反映「设置里指定的那台」，
  与菜单上的数字保持一致
- macOS 菜单栏项改由自建的 `NSStatusItem` 实现（原先是 SwiftUI 的 `MenuBarExtra`）——
  `MenuBarExtra` 只响应点击，拿不到状态项按钮就无法追踪鼠标悬停
- **macOS 版可经 Apple 公证分发**：`NOTARIZE=1 ./scripts/build-app.sh` 会自动完成
  签名、公证与票据装订，下载到的 .app 不再被 Gatekeeper 以「无法验证开发者」拦下。
  构建默认启用 hardened runtime

## [1.0.0] - 2026-09-08

首个公开版本。macOS 与 Windows 两端功能对等，共用同一份配置格式和本地数据库结构。

### 新增

**核心能力**

- 监控多台 VPS 的月度流量配额，按当前速度外推账期末用量并判断是否会超额
- 两条采集路径归一为「按天的 rx/tx 序列」：
  - **Vultr** —— 官方 REST API（`/v2/instances` 取 `allowed_bandwidth`，
    `/v2/instances/{id}/bandwidth` 取每日进出字节数）
  - **SSH + vnstat** —— 适用于任何可 SSH 的服务器（DMIT 无公开 API，走这条）
- 本地 SQLite 覆盖式 upsert 留存历史，不受上游数据窗口限制
  （Vultr 约 30 天、vnstat 默认 62 天）
- 按账单重置日切分账期，`resetDay` 支持 1–31 并在当月天数不足时回退到月末
- 四种计费口径（仅出站 / 仅入站 / 双向求和 / 取较大值）与两种 GB 进制（1024³ / 1000³）
- 并发采集，单台失败不影响其他台，失败原因与上次成功时间可见
- **起始已用量补偿** —— 账期中途才接入时，可从服务商面板抄一个已用量补齐缺口。
  该基准绑定具体账期，换账期后自动失效
- API Key 不写入配置文件：macOS 存系统钥匙串，Windows 用 DPAPI 加密

**macOS 端**

- 菜单栏常驻，图标直接显示最紧张那台的用量百分比
- 主窗口（服务器列表 + 详情与趋势图）
- **三条兜底入口** —— 菜单栏拥挤时 macOS 会静默丢弃放不下的状态项（带刘海的机型尤其
  容易触发），因此另有 Dock 图标与 `open -a VPSQuota`；「呈现方式」可在
  仅菜单栏 / 仅 Dock / 两者之间切换，默认两者都开
- 应用图标由代码绘制（`scripts/make-icon.swift`），小尺寸下自动简化图形
- 「帮助 → 使用说明」打开随包分发的文档；打包时若有 pandoc 则渲染为带样式的 HTML，
  没有则退回原始 Markdown
- 简体中文本地化（含系统标准菜单）
- 诊断 CLI `vpsquota-cli`：`set-key` / `vultr-instances` / `probe` / `refresh` /
  `status` / `config-path`，接入真实凭据时可绕开界面逐项排查

**Windows 端**

- 系统托盘常驻，图标颜色随用量变化
- 主窗口与设置界面，功能与 macOS 端对等

### 修复

- 设置界面「常规设置」点不动 —— 它的 tag 与「什么都没选」是同一个值，
  SwiftUI 无从区分。改用独立枚举消除二义性
- 设置界面左右比例失衡，改用 `NavigationSplitView` 并显式约束列宽
- 趋势图横轴在只有一天数据时退化成小时轴，改为固定按账期范围显示；
  账期内缺失的日子保留为空白 —— 数据缺口本身就是需要看见的信息
- 累计曲线未计入起始已用量，导致与头部数字打架，改为从基准起步
- 单日数据画不出线段，补 `PointMark`
- 帮助菜单指向一本并不存在的帮助书，点击只弹 "Help isn't available for …"
- 主窗口默认选中等在整轮采集之后，打开会空白好几秒；改为本地数据一到就选中
- 设置窗口在启动时自动弹出，用 `defaultLaunchBehavior(.suppressed)` 抑制

### 已知限制

- **不是对账工具。** 读的是网卡计数或 API 报告值，与服务商交换机上的计费计数
  通常接近但不相等。逼近配额时以服务商面板为准
- `meterMode` 与 `unitBase` 填错会造成系统性偏差（前者可能差一倍，后者差 7.4%），
  首次接入必须对照服务商面板校准
- vnstat 按服务器本地时区切分自然日，本工具按 UTC 记账，非 UTC 服务器有数小时偏差
- macOS 端为 ad-hoc 签名，每次重新构建签名都会变，首次读取钥匙串时会弹授权框
- Windows 端的 DPAPI 密文换机器或换用户后无法解开，需要重新填写 API Key
- Windows 端暂无自动化测试

[1.0.0]: https://github.com/striver2006/vps-traffic-quota/releases/tag/v1.0.0
