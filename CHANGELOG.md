# 更新日志

本文件记录值得用户关注的变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-10

首个正式公开版本。跨平台（macOS / Windows）VPS 月度流量用量监控状态栏工具，两端功能对等，共用同一份配置格式和本地数据库结构。

### 新增与优化

**核心监控与采集**
- 监控多台 VPS 的月度流量配额，按当前速度外推账期末用量并判断是否会超额
- 两条采集路径归一为「按天的 rx/tx 序列」：
  - **Vultr** —— 官方 REST API（`/v2/instances` 取 `allowed_bandwidth`，`/v2/instances/{id}/bandwidth` 取每日进出字节数，支持实例列表翻页）
  - **SSH + vnstat** —— 适用于任何可 SSH 的服务器（DMIT 无公开 API，走这条；vnStat 1.x 不兼容时提供友好升级引导）
- 本地 SQLite 覆盖式 upsert 留存历史，不受上游数据窗口限制（Vultr 约 30 天、vnstat 默认 62 天）
- 采集错误与上游配额持久化留存本地：离线或故障时继续显示上次原因和数据
- 按账单重置日切分账期，`resetDay` 支持 1–31 并在当月天数不足时回退到月末；账期切换当天自动翻页重算
- 四种计费口径（仅出站 / 仅入站 / 双向求和 / 取较大值）与两种 GB 进制（1024³ / 1000³）
- 并发采集，单台失败不影响其他台，失败原因与上次成功时间可见
- **起始已用量补偿** —— 账期中途才接入时，可从服务商面板抄一个已用量补齐缺口，换账期后自动失效
- 凭据安全存储：macOS 存系统钥匙串，Windows 用 DPAPI 加密

**桌面与交互**
- **悬停浮出面板**：鼠标移到菜单栏图标 / 托盘图标上即浮出面板，移开或点击面板外即收起；点击图标可钉住面板
- **常驻区剩余流量展示**：菜单栏 / 托盘图标旁直接显示指定服务器本账期的剩余流量（如 `1.94T`、`576G`、`512M`），可指定服务器或选择不显示
- **开机自启动**：设置中一键开启开机自启（macOS 经 `SMAppService`，Windows 写用户注册表 Run 项，均免管理员权限）
- **多入口与托盘适配**：
  - macOS 支持自建 `NSStatusItem` 菜单栏常驻、Dock 图标与主窗口双入口；支持 Apple Developer ID 签名与公证
  - Windows 支持系统托盘悬停浮窗、右键菜单摘要、多屏高 DPI 自适应及单实例防多开保护
- 历史趋势图：每日柱状与账期累计曲线双轴呈现
- 随包文档与诊断 CLI（macOS 端 `vpsquota-cli`）

**自动化测试与跨端契约**
- 双端对称单元测试（macOS Swift Test + Windows `windows/VpsQuota.Tests`），CI 自动化运行
- 账期切分、口径折算、起始补偿、表结构及配置解析跨端逐字对齐校验

### 已知限制

- **不是对账工具**：读的是网卡计数或 API 报告值，与服务商交换机上的计费计数通常接近但不相等。逼近配额时以服务商面板为准
- `meterMode` 与 `unitBase` 填错会造成系统性偏差，首次接入需对照服务商面板校准
- vnstat 按服务器本地时区切分自然日，本工具按 UTC 记账，非 UTC 服务器有数小时偏差
- Windows 端的 DPAPI 密文换机器或换用户后无法解开，需重新填写 API Key

[1.0.0]: https://github.com/striver2006/vps-traffic-quota/releases/tag/v1.0.0
