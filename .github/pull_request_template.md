## 改了什么

<!-- 一两句说明，以及为什么这么改 -->

## 关联 issue

<!-- Fixes #123 / Refs #123，没有可删掉这一节 -->

## 双端同步

<!-- 这个项目 macOS 与 Windows 功能对等，共用逻辑的改动通常需要成对进行 -->

- [ ] 只涉及单端，另一端无需改动
- [ ] 两端都改了
- [ ] 只改了一端，另一端待补 —— 原因：

如果改了配置字段或数据库结构，`shared/` 下的事实源（`config.example.json` / `schema.sql`）
也更新了吗？

- [ ] 是
- [ ] 不涉及

## 验证

- [ ] `cd macos && swift build` 通过
- [ ] `cd macos && swift test` 全绿
- [ ] 实机验证过（说明在哪个系统版本上、验证了什么）：
- [ ] 相关文档已同步更新（README / `docs/`）

## 检查

- [ ] 没有把真实凭据、真实服务器地址写进代码、测试或提交信息
